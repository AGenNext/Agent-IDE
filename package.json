{
  "name": "agent-ide",
  "private": true,
  "workspaces": [
    "applications/*",
    "extensions/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -A run build",
    "start": "yarn workspace browser-app start"
  }
}